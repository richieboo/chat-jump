{
  "name": "sms-contact-filter",
  "version": "1.0.0",
  "description": "A Chrome extension to filter SMS contacts.",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && mkdir dist && cpx \"{background.js,content.js,manifest.json}\" dist && cpx \"icons/**/*\" dist/icons"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cpx": "^1.5.0"
  }
}
