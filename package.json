{
  "name": "sms-contact-filter",
  "version": "1.0.0",
  "description": "A Chrome extension to filter SMS contacts.",
  "scripts": {
    "clean": "rimraf dist",
    "build": "rimraf dist && cpx \"{background.js,content.js,manifest.json,info.html,icons/*}\" dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cpx": "^1.5.0",
    "rimraf": "^6.1.0"
  }
}
